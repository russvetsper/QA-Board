
import { boardPopularity } from 'qa-board/helpers/board-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | board popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = boardPopularity([42]);
  assert.ok(result);
});


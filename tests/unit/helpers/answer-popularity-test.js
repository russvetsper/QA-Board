
import { answerPopularity } from 'qa-board/helpers/answer-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | answer popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerPopularity([42]);
  assert.ok(result);
});

